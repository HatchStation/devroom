<% conversation = "#conversation_#{@conversation.id.to_s}" %>

<% publish_to "/conversations/#{@conversation.id}/messages" do %>
  $("<%= conversation %> .messages").append("<%= j render(@message) %>");
<% end %>

$("<%= conversation %> .new_message textarea[name='message[text]']").val('');